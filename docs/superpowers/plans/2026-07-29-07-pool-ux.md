# Pool UX batch — owner-requested, post-launch

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Four tasks, one branch (`rohan/pool-ux`), each independently shippable.

**Goal:** Four owner-requested gaps found by using the live product: folder
upload, upload-status visibility, a mobile scrubber, and per-track
download/upload counts.

**Origin:** direct owner feedback 2026-07-29 after first real use. Verbatim
intents: "select a folder and recursively travel"; "no way for a user to know
what's the status of the tracks he uploaded"; "on the mobile phone there is
no scrubber at all"; "download count and upload count for each track ID,
agnostic of the format".

## Global constraints (inherited, unchanged)

All of CLAUDE.md applies. Highlights binding this batch: brutalist idiom —
server-rendered pages, GET forms, islands only where M2 already has them
(the upload pipeline); JSON `{error}` failures; revoke-first grants +
pgTAP 42501; 4-arg `throws_ok`, one `plan()` per file; definer functions
check ownership internally; MIT headers; migration timestamps after
`20260729130300` (label `_15_`; M4's plan drafted against 15 — its numbering
shifts at implementation, noted for the M4 PR review).

---

### Task 1: Folder upload — recursive traversal

**Files:** create `src/lib/dir-walk.ts` + `src/lib/dir-walk.test.ts`; modify
the dropzone component (`src/components/UploadDropzone.tsx`) and
`src/pages/upload.astro` (add a "select folder" input).

Mechanics:
- Drop path: `DataTransferItem.webkitGetAsEntry()` → recursive walk of
  directory entries. **The classic bug is `createReader().readEntries()`
  returning at most ~100 entries per call — it MUST be called in a loop
  until it yields an empty batch.** A unit test with a fake reader yielding
  100+100+3 entries is mandatory.
- Picker path: a second `<input type="file" webkitdirectory>` alongside the
  existing file input (folder pickers cannot mix with file pickers in one
  input).
- Filter: accept only extensions the upload policy already allowlists —
  import from `src/lib/upload-policy.ts`, do not duplicate the list. Skip
  dotfiles, `__MACOSX`, zero-byte files. Depth cap 12; file-count guard at
  2,000 with a clear message (the policy's own batch ceiling governs).
- Output: a flat `File[]` fed into the EXISTING pump/journal pipeline
  untouched — the walker only discovers files; dedupe of already-journaled
  files is already the journal's job.
- Walker is pure-testable in node with faked entry objects; the browser drop
  path is a manual check.

### Task 2: "My uploads" status page

**Files:** create `src/pages/uploads.astro`; migration
`…_15a` RPC `my_files(p_cursor …)` (`security definer`, `auth.uid()` scoped,
revoke-first) joining `files` × `ingest_jobs.last_error` ×
`audio_analysis(bpm, key_camelot)`; pgTAP proving another member's rows are
invisible; AppNav link "my uploads".

- Server-rendered table, newest first, grouped by batch: filename, state
  chip, failure reason verbatim when `failed` (the owner hit `empty_decode`
  today with no way to see it), bpm/key once `stored`, uploaded-at.
- Non-terminal rows get the existing `FileStateTicker` treatment (it already
  polls batch status — reuse, do not duplicate).
- This page is the answer to "what have I uploaded"; the pool answers "what
  does everyone have".

### Task 3: Mobile scrubber

**Files:** modify `src/layouts/Shell.astro` (the persisted
`<audio id="player-audio" controls>` bar) + the player script + css.

- Native `controls` renders inconsistently on mobile and the bar's layout
  hides seek affordance. Replace visible native controls with a minimal
  custom row: play/pause button, `elapsed / total` text (reuse
  `formatDuration`), and an `<input type="range">` seek bound to
  `currentTime` — range inputs are touch-friendly and need no library.
  Keep the `<audio>` element (no `controls` attr) as the engine; the
  persistence-across-navigation behaviour must not regress.
- `timeupdate` throttled to 1 Hz for the text; range `input` seeks.
- Works at 375px width without overflow. Manual checks: iOS Safari + Android
  Chrome (owner's phone) — desktop behaviour unchanged.

### Task 4: Download + upload counts (format-agnostic-ready)

**Files:** migration `…_15_track_stats.sql`; modify
`src/pages/api/track/[id]/download.ts`; surface in `pool_tracks`
view/`pool_list` + detail page; pgTAP.

- `track_stats(file_id uuid pk references files on delete cascade,
  download_count bigint not null default 0)`. Definer fn
  `bump_download(p_file uuid)` — upsert increment; grant execute to
  authenticated (any member's download counts); revoke-first; the download
  route calls it fire-and-forget AFTER minting the signed URL (a stats
  failure must never block a download).
- **Upload count is not a new counter** — it is `count(*) from file_claims`
  per file, the mechanism M2 built for exactly this. Expose it in the view.
- Format-agnostic by design, not yet by data: counts key on `file_id` today
  because the canonical track identity does not exist until M4. **CARRY TO
  M4 (must appear in its plan PR): the merge operation folds
  `download_count` (sum) and claims onto the surviving identity, inside the
  same reversible merge event.** This is the owner's stated intent — counts
  belong to the track, not the encode.
- Detail page shows both counts; pool table shows downloads.

---

## Order and verification

1 → 2 → 4 → 3 (3 is pure frontend, lowest risk, benefits from everything
else being merged). Full gates per task (vitest / pgTAP / check / build).
Deploy at the end of the batch (migrations via direct psql per CLAUDE.md,
then `npm run deploy`). Manual checklist at close: folder-drop a nested
directory, watch /uploads live during analysis, scrub on a phone, download
twice and watch the count.
